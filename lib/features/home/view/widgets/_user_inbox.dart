import 'package:flutter/cupertino.dart';

class UserInbox extends StatelessWidget {
  const UserInbox({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("User Inbox"),
    );
  }
}
